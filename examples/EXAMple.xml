<xml>

<env>exam</env>

<settings>
decimal_separator = "{,}"
task_join = "\vspace{\baselineskip}"
</settings>

<variables>
TASK_FILES = []
TASK_FILES.append("train_gradient")
TASK_FILES.append("car_gradient")
TASK_FILES = shuffle(list(map(lambda s: "EXAMple/%s.xml" % s, TASK_FILES)))

g = 9.81

date = (23, 2, 2022)
subject = "Grundlegendes Allgemeinwissen - Prüfung 1"
empty = "\\vspace{\\baselineskip}"

# generate sketches
</variables>

<latexfile>
EXAMple.jtex
</latexfile>

</xml>
